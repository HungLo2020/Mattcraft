
**Goal 1 — Vanilla Rust Vulkan:** Incrementally and safely complete MattMC’s vanilla rendering migration so selecting Vulkan renders the full vanilla game exclusively through semantic rendering callsites, the Rust-implemented explicit VulkanicGAL, and the Rust Vulkan backend, with no Java Vulkan rendering or fallback, borrowed Iris GPU state, shared native GPU handles, or second presenter; cover all vanilla terrain/fluids/translucency, GUI/HUD/text, entities and layers, block entities, items and first-person hands, particles, sky/clouds/weather, world effects, post-processing, resource packs, menus/loading screens, and every remaining vanilla rendering callsite, while explicitly excluding Distant Horizons and Iris shader-pack execution except that neither may contaminate vanilla ownership. Frozen Java OpenGL in the separate Frozen repository is the sole correctness baseline; use the shared cross-repository parity harness with equivalent fixtures, worlds, camera state, settings, packs, timing, diagnostics, and captures, never use Frozen/current Java Vulkan for correctness, parity, acceptance, or normal testing, never change Frozen behavior, and stop for my decision before treating suspected Frozen behavior as a bug. Preserve the intended architecture: callsites provide semantic game/resource data, VulkanicGAL owns explicit backend-neutral resources/pipelines/passes/usages/synchronization/submission/completion/presentation, Rust Vulkan consumes that state directly, transitional Java may only extract immutable semantics rather than become a renderer or policy engine, and only Rust OpenGL may reconstruct implicit GL state. Prefer narrow complete vertical slices, treat every discovered prerequisite as implementation work, keep incomplete/incorrect Rust capabilities normally unadmitted rather than falling back, and do not stop at audits or merely report blockers when safe implementation/debugging work remains. A capability is complete only after a real end-to-end Rust Vulkan run, deterministic paired Frozen OpenGL capture with equivalent conditions, semantic ownership/no-Java-Vulkan/no-Iris-GPU-state evidence, and bounded-resource validation; source tests and synthetic scenes are supplemental only, never weaken parity thresholds/evidence/capture requirements to accommodate defects, use temporary architecture-preserving diagnostics to isolate and fix faults rather than repeatedly reporting them, watch host memory during tests and investigate/fix abnormal memory growth instead of treating OOM alone as a blocker, and do not commit or push.

**Goal 2 — Distant Horizons on Vanilla Rust Vulkan:** Starting from the completed vanilla Rust Vulkan route, incrementally and safely make Distant Horizons render correctly through the same single Rust-owned Vulkan frame so normal Vulkan gameplay with Distant Horizons enabled uses semantic DH callsites/data, explicit VulkanicGAL state, and the Rust Vulkan backend exclusively, with no Java Vulkan rendering/fallback, DH OpenGL/Vulkan presenter, borrowed Iris/DH GPU state, shared native GPU handles, or second presenter; support opaque, translucent, water, LOD materials, lighting/fog/fading, depth interaction, resource-pack behavior, world transitions, rebuilds, and every DH rendering callsite required for normal gameplay, while shader-pack execution remains outside this phase except that Iris must not become a dependency of the DH Rust path. Frozen Java OpenGL with the equivalent Distant Horizons setup in the separate Frozen repository is the sole correctness baseline; use the shared cross-repository parity harness with equivalent worlds, LOD state, camera, settings, packs, timing, diagnostics, and captures, never use Frozen/current Java Vulkan for correctness, parity, acceptance, or normal testing, never change Frozen behavior, and stop for my decision before treating suspected Frozen behavior as a bug. Preserve the explicit architecture: DH/Java may expose immutable semantic CPU data but must not pass renderer objects, GPU resources, native handles, GL state, or rendering policy across the boundary; VulkanicGAL owns explicit resources/pipelines/passes/usages/synchronization/submission/completion/presentation and Rust Vulkan consumes them directly. Prefer narrow complete vertical slices, treat every prerequisite as implementation work, keep incomplete/incorrect DH capabilities unadmitted rather than falling back, and do not stop at audits or merely report blockers when safe implementation/debugging remains. A DH capability is complete only after a real end-to-end Rust Vulkan+DH run, deterministic paired Frozen OpenGL+DH capture under equivalent conditions, semantic ownership/no-Java-Vulkan/no-borrowed-DH-or-Iris-GPU-state evidence, and bounded-resource validation; source tests/synthetic scenes are supplemental only, never relax parity/evidence/capture gates to accommodate failures, use temporary architecture-preserving diagnostics to isolate and fix defects, watch memory carefully and fix abnormal Rust/DH memory growth rather than treating OOM alone as a blocker, and do not commit or push.

**Goal 3 — Iris Shader Packs on Rust Vulkan:** Starting from the completed vanilla Rust Vulkan route, incrementally and safely implement normal Iris shader-pack compatibility entirely inside the Rust-owned renderer so selecting Vulkan with a supported shader pack renders the full non-Distant-Horizons game through semantic callsites, copied shader-pack source/assets/configuration, Rust-owned preprocessing/lowering/runtime pass policy, explicit VulkanicGAL, and the Rust Vulkan backend, with no Java Vulkan rendering/fallback, Iris GPU programs/framebuffers/textures/buffers/callbacks/runtime rendering state, shared native handles, or second presenter; support required terrain, entities, hand, particles, sky/clouds/weather, shadows, deferred/composite/final passes, post-processing, custom textures/resources/options/uniform semantics, resource packs, and every shader-affected rendering callsite needed for correct gameplay, while Distant Horizons integration is outside this phase. Frozen Java OpenGL running the same Iris shader pack in the separate Frozen repository is the sole correctness baseline; use the shared cross-repository parity harness with identical pack, options, resource pack, fixture/world, camera, settings, timing, diagnostics, and captures, never use Frozen/current Java Vulkan for correctness, parity, acceptance, or normal testing, never change Frozen behavior, and stop for my decision before treating suspected Frozen behavior as a bug. Preserve the explicit architecture: Java/Iris may only provide bounded immutable shader source/assets/config semantics where still transitional, while shader parsing/lowering, pass order, resources, synchronization, execution, and presentation belong to Rust/VulkanicGAL/Rust Vulkan; never reproduce compatibility by borrowing Iris GPU runtime state. Prefer narrow complete vertical slices, treat discovered prerequisites as implementation work, keep unsupported or incorrect source stages/packs unadmitted rather than silently substituting Java/Iris execution, and do not stop at audits or merely report blockers while safe implementation/debugging remains. A shader capability is complete only after a real end-to-end Rust Vulkan shader-pack run, deterministic paired Frozen OpenGL+Iris capture under equivalent conditions, semantic ownership/no-Java-Vulkan/no-Iris-GPU-state evidence, and bounded-resource/clean-validation evidence; source tests and synthetic shaders are supplemental only, never weaken parity thresholds/evidence/capture schedules to accommodate defects, use temporary architecture-preserving diagnostic shaders/readbacks/captures to isolate and fix faults, watch memory and fix abnormal shader/runtime resource growth instead of treating OOM alone as a blocker, and do not commit or push.

**Goal 4 — Iris Shader Packs + Distant Horizons Together:** Starting from separately working vanilla Rust Vulkan, Rust-owned Distant Horizons, and Rust-owned Iris shader-pack execution, incrementally and safely make Iris shader packs and Distant Horizons operate together in one coherent Rust Vulkan frame so selecting Vulkan with both enabled renders the complete supported game exclusively through semantic callsites, Rust-owned shader/DH semantics and resources, explicit VulkanicGAL, and the Rust Vulkan backend, with no Java Vulkan rendering/fallback, Iris or DH GPU-runtime borrowing, shared native GPU handles, alternate framebuffer/presenter ownership, or second presenter; correctly integrate DH opaque/translucent/water passes with shader-pack terrain/shadow/deferred/composite/final behavior, depth and attachment contracts, lighting/fog/material semantics, custom resources/options, resource packs, world transitions, and every interaction needed for correct gameplay. Frozen Java OpenGL with the same Iris pack, Distant Horizons configuration, resource pack, fixture/world, camera, settings, timing, diagnostics, and captures in the separate Frozen repository is the sole correctness baseline; use the shared cross-repository parity harness, never use Frozen/current Java Vulkan for correctness, parity, acceptance, or normal testing, never change Frozen behavior, and stop for my decision before treating suspected Frozen behavior as a bug. Preserve singular Rust ownership and the explicit architecture: Java/Iris/DH may supply bounded immutable semantics only, while rendering policy, pass graph, resources, pipelines, usages, synchronization, submission, completion, cross-system ordering, and presentation belong to Rust/VulkanicGAL/Rust Vulkan; never solve integration by importing Iris/DH GPU state or allowing either subsystem to present independently. Prefer narrow complete integration slices, treat every discovered prerequisite as implementation work, keep incomplete combinations unadmitted rather than falling back, and do not stop at audits or merely report blockers while safe implementation/debugging remains. Completion requires a real end-to-end Rust Vulkan+Iris+DH run, deterministic paired Frozen OpenGL+Iris+DH capture under equivalent conditions, semantic ownership/no-Java-Vulkan/no-Iris-or-DH-GPU-state evidence, clean validation, and bounded resources; source tests/synthetic scenes are supplemental only, never relax parity/evidence/capture gates to accommodate defects, use temporary architecture-preserving diagnostics to isolate and fix faults rather than just report them, watch host memory and fix abnormal combined-system memory growth rather than treating OOM alone as a blocker, and do not commit or push.
